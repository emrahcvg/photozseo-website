# Uygulama Planı — Alt-proje #3 (Web Store/Market Render Adaptasyonu) + #5 Web Tarafı (Lazy categoryId Map)

**Tarih:** 2026-06-01
**Branch:** `feat/taxonomy-pipeline`
**Kapsam:** Bu plan SADECE web tarafını (`/store` + `/market` render + lazy legacy-map) kapsar. iOS (#4) ayrı repo. Kilitli kararlara göre #3 render ile #5 lazy-map **atomik tek sürümde** gider.
**Önkoşul kararlar:** `docs/superpowers/specs/2026-06-01-resolved-decisions.md` (#3 + #5 bölümleri kilitlendi).

---

## Bağlam ve Mevcut Durum (kod incelemesi)

Render katmanı bugün `taxonomy.json`'u import etmiyor; iki ayrı kategori yolu var:

- **Store (`/store/<slug>`):** Kategori verisi manifest içinde (`Manifest.categories: {id, name: Localized}[]`). Canlı render yolu **`src/storefront/render.ts`** içindeki saf-TS `renderStoreBody`/`renderProductBody` (Pages Functions — `functions/store/[[path]].ts` bunları çağırır). `.astro` bileşenleri (`StorePage`, `CategorySection`, `ProductDetail`) Astro build yolunun aynaları; ikisi de güncellenir. `groupProductsByCategory` (manifest.ts:132-141) ürünleri `product.categoryId === category.id` **string eşitliği** ile gruplar; eşleşmeyen → `category: null` ("Diğer/Other").
- **Market (`/market`):** D1 satırları (`ProductRow.category_id` = ham id). `marketplace.ts` içindeki `renderCategoryChips`/`renderFacets`/`renderCategoryPage`/`renderMarketHome` kategori adını **çevirmeden ham `c.id`'yi** basıyor (`escapeHtml(c.id)`). **DÜZELTİLECEK BUG:** `/market/c/electronics.phones` ekranda "electronics.phones" yazıyor; label çözümü yok.

**Kanonik veri (#1 çıktısı — `build:taxonomy` script `scripts/build-taxonomy.mjs`):**
- `src/storefront/taxonomy/tree.json` → `[{id, parentId, depth}]` (~5595 düğüm, numeric-string id).
- `src/storefront/taxonomy/labels.<lang>.json` → `{id: leafLabel}` (her düğüm, 12 dil).
- `src/storefront/taxonomy/meta.json` → `{googleVersion, taxonomyVersion, generatedAt}`.

> **KRİTİK DEPENDENCY CAVEAT:** Bu plan yazılırken `src/storefront/taxonomy/` **henüz yok** (#1 build tamamlanmadı). Bu yüzden tüm saf servis mantığı **commit'li küçük fixture'larla** (`src/storefront/taxonomy/__fixtures__/`) TDD edilir; gerçek tree/labels JSON'larına **bağımlı değil**. Entegrasyon (gerçek import) #1 çıktısı geldiğinde Task 9'da doğrulanır. #2 API kontratı referans alınır ama **kilitli karar gereği web statik import kullanır (HTTP değil)**.

**Veri servis modeli (kilitli):** Web = build-time/edge **static import**, KV upload yok. Tek kanonik kaynak #1'in `taxonomy/` çıktısı.

---

## File Structure

```
src/storefront/taxonomy/
  service.ts                    # YENİ — saf-TS taxonomyService (node/children/ancestors/label/path)
  service.test.ts               # YENİ — service unit testleri (fixture ile)
  legacy-map.ts                 # YENİ — legacy-map.json loader + isGoogleId + mapLegacyId (saf)
  legacy-map.json               # YENİ — {oldId: googleId} (~31 satır, web+iOS ortak)
  legacy-map.test.ts            # YENİ — mapLegacyId + idempotency + tree doğrulama (CI hard-stop)
  category-resolve.ts           # YENİ — resolveCategoryName + breadcrumb path helper (store+market ortak)
  category-resolve.test.ts      # YENİ
  __fixtures__/
    tree.fixture.json           # YENİ — ~12 düğümlük mini ağaç (depth 0..3)
    labels.en.fixture.json      # YENİ
    labels.tr.fixture.json      # YENİ

src/storefront/
  marketplace.ts                # DEĞİŞ — label inject (labelOf), breadcrumb, buildBreadcrumbJsonLd
  marketplace.test.ts           # DEĞİŞ — label-inject testleri
  manifest.ts                   # DEĞİŞ — groupProductsByCategory taksonomi-fallback + resolveCategoryName entegrasyon
  manifest.test.ts              # DEĞİŞ/YENİ — grup fallback testleri
  render.ts                     # DEĞİŞ — store breadcrumb (renderProductBody) + label
  render.test.ts                # DEĞİŞ — breadcrumb render testleri
  market-sitemap.ts             # YENİ DOSYA — ürün-barındıran L1 sitemap helper (saf) [not: bugünkü dosya functions/marketplace-sitemap.ts]

functions/market/[[path]].ts    # DEĞİŞ — taxonomyService kur, labelOf inject, breadcrumb JSON-LD, facet drill reduce
functions/marketplace-sitemap.ts# DEĞİŞ — ürün-barındıran L1 kategori URL'leri ekle
functions/_lib/marketplace.ts   # DEĞİŞ — lazy legacy-map okuma (category_id) + Orama'ya kategori label gömme
functions/_lib/registry.ts      # DEĞİŞ — putStore'da write-normalize (idempotent) [#5 dual-write]

src/components/storefront/
  CategorySection.astro         # DEĞİŞ — resolveCategoryName (manifest adı → svc → Other)
  ProductDetail.astro           # DEĞİŞ — breadcrumb satırı (svc.path), çözülemezse gizli
  StorePage.astro               # DEĞİŞ — svc prop geçişi (groups fallback)
```

> **NOT (market-sitemap.ts):** Görev `src/storefront/market-sitemap.ts`'e atıf yapıyor ama mevcut sitemap kodu `functions/marketplace-sitemap.ts`'te. Saf, test-edilebilir `buildMarketplaceSitemap` zaten orada export'lu. Yeni saf helper'ı **`src/storefront/market-sitemap.ts`** olarak ekliyoruz (taxonomyService'e bağımlı, test edilebilir) ve `functions/marketplace-sitemap.ts` router'ından çağırıyoruz. Bu, "render saf-TS, functions wiring" deseniyle tutarlı.

---

## Görev Sırası ve Bağımlılıklar

```
Task 1 (service) ──┬─► Task 3 (category-resolve) ──┬─► Task 4 (manifest grup)
Task 2 (legacy-map)┘                                ├─► Task 5 (market label inject)
                                                    ├─► Task 6 (store render breadcrumb)
                                                    └─► Task 7 (astro components)
Task 5 ─► Task 8 (market router wiring + facet drill + breadcrumb JSON-LD)
Task 2 ─► Task 10 (lazy map okuma _lib/marketplace) ─► Task 11 (write-normalize registry)
Task 1 ─► Task 9 (sitemap L1)
Task 9,8,11 ─► Task 12 (entegrasyon doğrulama — #1 çıktısı geldiğinde)
```

Her görev TDD: önce test (kırmızı), sonra implementasyon (yeşil), sonra commit.

---

## Task 1 — Saf `taxonomyService` (tree + label index)

**Amaç:** `node/children/ancestors/label/path/version` sağlayan saf-TS servis. DOM yok, `marketplace.ts` ile aynı test standardı. Lang label haritaları lazy.

**Files:**
- YENİ: `src/storefront/taxonomy/__fixtures__/tree.fixture.json`
- YENİ: `src/storefront/taxonomy/__fixtures__/labels.en.fixture.json`
- YENİ: `src/storefront/taxonomy/__fixtures__/labels.tr.fixture.json`
- YENİ: `src/storefront/taxonomy/service.ts`
- YENİ: `src/storefront/taxonomy/service.test.ts`

### Adım 1.1 — Fixture'lar (commit'li mini ağaç)

`src/storefront/taxonomy/__fixtures__/tree.fixture.json`:
```json
[
  { "id": "222", "parentId": null, "depth": 0 },
  { "id": "1604", "parentId": null, "depth": 0 },
  { "id": "267", "parentId": "5181", "depth": 3 },
  { "id": "5181", "parentId": "262", "depth": 2 },
  { "id": "262", "parentId": "222", "depth": 1 },
  { "id": "187", "parentId": "166", "depth": 1 },
  { "id": "166", "parentId": null, "depth": 0 }
]
```

`src/storefront/taxonomy/__fixtures__/labels.en.fixture.json`:
```json
{
  "222": "Electronics",
  "1604": "Clothing",
  "262": "Communications",
  "5181": "Telephony",
  "267": "Mobile Phones",
  "166": "Apparel & Accessories",
  "187": "Shoes"
}
```

`src/storefront/taxonomy/__fixtures__/labels.tr.fixture.json`:
```json
{
  "222": "Elektronik",
  "1604": "Giyim",
  "262": "İletişim",
  "5181": "Telefon",
  "267": "Cep Telefonları",
  "166": "Giyim & Aksesuar",
  "187": "Ayakkabı"
}
```

### Adım 1.2 — Test (kırmızı)

`src/storefront/taxonomy/service.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { createTaxonomyService, type TaxNode } from './service';
import tree from './__fixtures__/tree.fixture.json';
import labelsEn from './__fixtures__/labels.en.fixture.json';
import labelsTr from './__fixtures__/labels.tr.fixture.json';

const svc = createTaxonomyService({
  tree: tree as TaxNode[],
  labels: { en: labelsEn as Record<string, string>, tr: labelsTr as Record<string, string> },
  taxonomyVersion: 7,
});

describe('taxonomyService.node', () => {
  it('var olan düğümü döner', () => {
    expect(svc.node('267')).toEqual({ id: '267', parentId: '5181', depth: 3 });
  });
  it('olmayan düğümde undefined döner (throw etmez)', () => {
    expect(svc.node('electronics.phones')).toBeUndefined();
  });
});

describe('taxonomyService.children', () => {
  it('null → depth-0 kökleri döner', () => {
    expect(svc.children(null).map((n) => n.id).sort()).toEqual(['1604', '166', '222']);
  });
  it('bir düğümün doğrudan çocuklarını döner', () => {
    expect(svc.children('222').map((n) => n.id)).toEqual(['262']);
  });
  it('çocuğu olmayan düğümde boş dizi döner', () => {
    expect(svc.children('267')).toEqual([]);
  });
});

describe('taxonomyService.ancestors', () => {
  it('kökten id-1\'e sıralı atalar (id hariç)', () => {
    expect(svc.ancestors('267').map((n) => n.id)).toEqual(['222', '262', '5181']);
  });
  it('kök düğümün atası yok', () => {
    expect(svc.ancestors('222')).toEqual([]);
  });
  it('olmayan düğümde boş dizi (throw etmez)', () => {
    expect(svc.ancestors('nope')).toEqual([]);
  });
});

describe('taxonomyService.label', () => {
  it('istenen dilde label döner', () => {
    expect(svc.label('267', 'tr')).toBe('Cep Telefonları');
  });
  it('eksik dilde en\'e düşer', () => {
    expect(svc.label('267', 'de')).toBe('Mobile Phones');
  });
  it('hiç label yoksa id\'ye düşer (throw etmez)', () => {
    expect(svc.label('999', 'en')).toBe('999');
  });
});

describe('taxonomyService.path', () => {
  it('atalar + self label dizisi (breadcrumb)', () => {
    expect(svc.path('267', 'en')).toEqual(['Electronics', 'Communications', 'Telephony', 'Mobile Phones']);
  });
  it('kök düğümde tek elemanlı', () => {
    expect(svc.path('222', 'tr')).toEqual(['Elektronik']);
  });
  it('olmayan düğümde ham id\'yi tek eleman döner (kırılmaz)', () => {
    expect(svc.path('electronics.phones', 'en')).toEqual(['electronics.phones']);
  });
});

describe('taxonomyService.version', () => {
  it('meta taxonomyVersion döner', () => {
    expect(svc.version()).toBe(7);
  });
});
```

### Adım 1.3 — Implementasyon (yeşil)

`src/storefront/taxonomy/service.ts`:
```ts
/**
 * service.ts — Saf-TS taxonomyService. tree.json + labels.<lang>.json üzerinde
 * okuma yüzeyi. DOM yok; store (build/SSR) ve market (edge) tüketir.
 * Çözülemeyen id'de ASLA throw etmez — her zaman zarif düşüş (id'ye fallback).
 */

export interface TaxNode {
  id: string;
  parentId: string | null;
  depth: number;
}

export interface TaxonomyService {
  node(id: string): TaxNode | undefined;
  children(id: string | null): TaxNode[];
  ancestors(id: string): TaxNode[];
  label(id: string, lang: string): string;
  path(id: string, lang: string): string[];
  version(): number;
}

export interface TaxonomyData {
  tree: TaxNode[];
  labels: Record<string, Record<string, string>>;
  taxonomyVersion: number;
}

const ROOT_KEY = '__root__';
const FALLBACK_LANG = 'en';

export function createTaxonomyService(data: TaxonomyData): TaxonomyService {
  const byId = new Map<string, TaxNode>();
  const childrenOf = new Map<string, TaxNode[]>();

  for (const n of data.tree) {
    byId.set(n.id, n);
    const key = n.parentId ?? ROOT_KEY;
    const arr = childrenOf.get(key) ?? [];
    arr.push(n);
    childrenOf.set(key, arr);
  }

  const node = (id: string): TaxNode | undefined => byId.get(id);

  const children = (id: string | null): TaxNode[] =>
    childrenOf.get(id ?? ROOT_KEY) ?? [];

  const ancestors = (id: string): TaxNode[] => {
    const chain: TaxNode[] = [];
    let cur = byId.get(id);
    // güvenlik: döngü/bozuk parentId'ye karşı ziyaret seti
    const seen = new Set<string>();
    while (cur && cur.parentId != null && !seen.has(cur.parentId)) {
      seen.add(cur.parentId);
      const parent = byId.get(cur.parentId);
      if (!parent) break;
      chain.push(parent);
      cur = parent;
    }
    return chain.reverse(); // kökten id-1'e
  };

  const label = (id: string, lang: string): string => {
    const table = data.labels[lang];
    if (table && table[id] != null) return table[id];
    const en = data.labels[FALLBACK_LANG];
    if (en && en[id] != null) return en[id];
    return id;
  };

  const path = (id: string, lang: string): string[] => {
    const chain = [...ancestors(id), ...(byId.has(id) ? [byId.get(id)!] : [])];
    if (chain.length === 0) return [label(id, lang)]; // çözülemeyen: ham id
    return chain.map((n) => label(n.id, lang));
  };

  const version = (): number => data.taxonomyVersion;

  return { node, children, ancestors, label, path, version };
}
```

### Komut + beklenen çıktı

```
npm test -- src/storefront/taxonomy/service.test.ts
```
Beklenen: `Test Files  1 passed (1)`, tüm `it` yeşil (16+ assertion).

### Commit
```
test+feat(taxonomy): saf taxonomyService (node/children/ancestors/label/path)
```

---

## Task 2 — Lazy `legacy-map` (eski id → google id) + idempotency

**Amaç:** Eski (`electronics.phones`) → numeric google id eşlemesi; `isGoogleId` + `mapLegacyId` saf fonksiyonlar; tree doğrulama (CI hard-stop). Web+iOS ortak `legacy-map.json`.

**Files:**
- YENİ: `src/storefront/taxonomy/legacy-map.json`
- YENİ: `src/storefront/taxonomy/legacy-map.ts`
- YENİ: `src/storefront/taxonomy/legacy-map.test.ts`

### Adım 2.1 — `legacy-map.json` (kilitli #5 tablosu)

```json
{
  "electronics": "222",
  "electronics.phones": "267",
  "electronics.computers": "278",
  "electronics.audio": "223",
  "clothing": "1604",
  "clothing.womens": "1604",
  "clothing.mens": "1604",
  "clothing.kids": "5424",
  "clothing.shoes": "187",
  "home": "536",
  "home.furniture": "436",
  "home.kitchen": "730",
  "home.decor": "696",
  "beauty": "469",
  "beauty.makeup": "2915",
  "beauty.skincare": "567",
  "beauty.fragrance": "2882",
  "jewelry": "188",
  "jewelry.rings": "194",
  "jewelry.necklaces": "196",
  "jewelry.watches": "201",
  "toys": "1239",
  "sports": "499713",
  "books": "784",
  "food": "412",
  "pets": "1",
  "automotive": "888",
  "baby": "537",
  "health": "491",
  "office": "922",
  "art": "505370"
}
```

### Adım 2.2 — Test (kırmızı)

`src/storefront/taxonomy/legacy-map.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { isGoogleId, mapLegacyId, UNCATEGORIZED } from './legacy-map';
import legacyMap from './legacy-map.json';

describe('isGoogleId', () => {
  it('saf numeric string için true', () => {
    expect(isGoogleId('267')).toBe(true);
    expect(isGoogleId('1')).toBe(true);
  });
  it('eski dot-id / serbest id için false', () => {
    expect(isGoogleId('electronics.phones')).toBe(false);
    expect(isGoogleId('c1')).toBe(false);
    expect(isGoogleId('')).toBe(false);
  });
});

describe('mapLegacyId', () => {
  const map = legacyMap as Record<string, string>;
  it('zaten google id ise dokunmaz (idempotent)', () => {
    expect(mapLegacyId('267', map)).toBe('267');
  });
  it('eski id\'yi google id\'ye çevirir', () => {
    expect(mapLegacyId('electronics.phones', map)).toBe('267');
    expect(mapLegacyId('clothing.shoes', map)).toBe('187');
  });
  it('serbest/bilinmeyen id → UNCATEGORIZED sentinel', () => {
    expect(mapLegacyId('c1', map)).toBe(UNCATEGORIZED);
    expect(mapLegacyId('rastgele', map)).toBe(UNCATEGORIZED);
  });
  it('boş/undefined → UNCATEGORIZED', () => {
    expect(mapLegacyId('', map)).toBe(UNCATEGORIZED);
    expect(mapLegacyId(undefined, map)).toBe(UNCATEGORIZED);
  });
  it('cinsiyet düğümleri ebeveyne (1604) çöker', () => {
    expect(mapLegacyId('clothing.womens', map)).toBe('1604');
    expect(mapLegacyId('clothing.mens', map)).toBe('1604');
  });
});

describe('legacy-map tablosu yapısı', () => {
  it('31 girdi içerir', () => {
    expect(Object.keys(legacyMap).length).toBe(31);
  });
  it('tüm hedefler google id (numeric string)', () => {
    for (const v of Object.values(legacyMap as Record<string, string>)) {
      expect(isGoogleId(v)).toBe(true);
    }
  });
});
```

### Adım 2.3 — Implementasyon (yeşil)

`src/storefront/taxonomy/legacy-map.ts`:
```ts
/**
 * legacy-map.ts — Eski (dot/serbest) categoryId → Google numeric id eşlemesi.
 * Saf; read-time (lazy) ve write-normalize aynı fonksiyonları kullanır.
 * Çözülemeyen id → UNCATEGORIZED sentinel (sessiz kayıp YOK; çağıran raporlar).
 */

export const UNCATEGORIZED = 'uncategorized';

/** Saf numeric string mi (Google taksonomi id formatı)? */
export function isGoogleId(id: string): boolean {
  return /^[0-9]+$/.test(id);
}

/**
 * Eski id'yi Google id'ye çevir. Zaten Google id ise dokunma (idempotent).
 * Haritada yoksa UNCATEGORIZED sentinel döner.
 */
export function mapLegacyId(id: string | undefined, map: Record<string, string>): string {
  if (!id) return UNCATEGORIZED;
  if (isGoogleId(id)) return id;
  return map[id] ?? UNCATEGORIZED;
}
```

### Adım 2.4 — Tree doğrulama testi (CI hard-stop, #1 çıktısı geldiğinde)

`legacy-map.test.ts`'e eklenir (gerçek `tree.json` yoksa **skip**, varsa **hard-stop**):
```ts
import { existsSync } from 'node:fs';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

describe('legacy-map → tree.json doğrulama (CI hard-stop)', () => {
  const treePath = fileURLToPath(new URL('./tree.json', import.meta.url));
  const ready = existsSync(treePath);
  const maybe = ready ? it : it.skip;

  maybe('her google_id tree.json\'da var olmalı', () => {
    const tree = JSON.parse(readFileSync(treePath, 'utf8')) as { id: string }[];
    const ids = new Set(tree.map((n) => n.id));
    const missing = Object.entries(legacyMap as Record<string, string>)
      .filter(([, gid]) => !ids.has(gid))
      .map(([old, gid]) => `${old}→${gid}`);
    expect(missing, `tree.json'da eksik hedefler: ${missing.join(', ')}`).toEqual([]);
  });
});
```
> **Caveat:** `tree.json` gelmeden bu test **skip**'tir; geldiğinde otomatik aktifleşir ve eksik id varsa CI fail eder. Tablodaki riskli id'ler (`267, 499713, 505370, 5424, 2882`) burada yakalanır.

### Komut + beklenen çıktı
```
npm test -- src/storefront/taxonomy/legacy-map.test.ts
```
Beklenen: tablo + map testleri yeşil; tree doğrulama `skipped` (tree.json yok). `Tests  N passed | 1 skipped`.

### Commit
```
test+feat(taxonomy): lazy legacy-map (isGoogleId/mapLegacyId) + CI tree doğrulama
```

---

## Task 3 — `resolveCategoryName` + breadcrumb path (store+market ortak)

**Amaç:** Öncelik zinciri `manifestCategory.name → svc.label → "Other"`; ve store breadcrumb path helper (parentId zinciri). Saf, svc inject.

**Files:**
- YENİ: `src/storefront/taxonomy/category-resolve.ts`
- YENİ: `src/storefront/taxonomy/category-resolve.test.ts`

### Adım 3.1 — Test (kırmızı)

`src/storefront/taxonomy/category-resolve.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { createTaxonomyService, type TaxNode } from './service';
import { resolveCategoryName, categoryBreadcrumb } from './category-resolve';
import tree from './__fixtures__/tree.fixture.json';
import labelsEn from './__fixtures__/labels.en.fixture.json';
import labelsTr from './__fixtures__/labels.tr.fixture.json';

const svc = createTaxonomyService({
  tree: tree as TaxNode[],
  labels: { en: labelsEn as Record<string, string>, tr: labelsTr as Record<string, string> },
  taxonomyVersion: 1,
});

describe('resolveCategoryName', () => {
  it('manifest adı varsa onu kullanır (öncelik 1)', () => {
    expect(
      resolveCategoryName('267', { en: 'My Custom Phones' }, 'en', svc),
    ).toBe('My Custom Phones');
  });
  it('manifest adı boşsa svc.label\'a düşer', () => {
    expect(resolveCategoryName('267', undefined, 'tr', svc)).toBe('Cep Telefonları');
    expect(resolveCategoryName('267', {}, 'en', svc)).toBe('Mobile Phones');
  });
  it('hiçbiri çözülmezse "Other"/"Diğer" döner', () => {
    expect(resolveCategoryName('c1', undefined, 'en', svc)).toBe('Other');
    expect(resolveCategoryName('c1', undefined, 'tr', svc)).toBe('Diğer');
  });
  it('null categoryId → "Other"', () => {
    expect(resolveCategoryName(undefined, undefined, 'en', svc)).toBe('Other');
  });
});

describe('categoryBreadcrumb', () => {
  it('çözülen id için tam yol + id segmentleri', () => {
    expect(categoryBreadcrumb('267', 'en', svc)).toEqual([
      { id: '222', label: 'Electronics' },
      { id: '262', label: 'Communications' },
      { id: '5181', label: 'Telephony' },
      { id: '267', label: 'Mobile Phones' },
    ]);
  });
  it('çözülemeyen id → null (breadcrumb gizlenir)', () => {
    expect(categoryBreadcrumb('electronics.phones', 'en', svc)).toBeNull();
    expect(categoryBreadcrumb(undefined, 'en', svc)).toBeNull();
  });
});
```

### Adım 3.2 — Implementasyon (yeşil)

`src/storefront/taxonomy/category-resolve.ts`:
```ts
/**
 * category-resolve.ts — Kategori adı çözümleme (store+market ortak).
 * Öncelik: manifest adı → taxonomy label → "Other". svc inject; saf.
 */
import type { TaxonomyService } from './service';
import { UNCATEGORIZED, isGoogleId } from './legacy-map';

type Localized = Record<string, string>;

function resolveLocalized(field: Localized | undefined, lang: string): string {
  if (!field) return '';
  return field[lang] ?? field.en ?? Object.values(field)[0] ?? '';
}

/** manifest adı → svc.label → "Other"/"Diğer". */
export function resolveCategoryName(
  categoryId: string | undefined,
  manifestName: Localized | undefined,
  lang: string,
  svc: TaxonomyService,
): string {
  const fromManifest = resolveLocalized(manifestName, lang).trim();
  if (fromManifest) return fromManifest;
  if (categoryId && categoryId !== UNCATEGORIZED && svc.node(categoryId)) {
    return svc.label(categoryId, lang);
  }
  return lang === 'tr' ? 'Diğer' : 'Other';
}

export interface BreadcrumbSegment {
  id: string;
  label: string;
}

/**
 * parentId zincirinden breadcrumb segmentleri. Çözülemeyen/boş id → null
 * (çağıran breadcrumb satırını GİZLER; kırık zincir gösterilmez).
 */
export function categoryBreadcrumb(
  categoryId: string | undefined,
  lang: string,
  svc: TaxonomyService,
): BreadcrumbSegment[] | null {
  if (!categoryId || categoryId === UNCATEGORIZED) return null;
  const self = svc.node(categoryId);
  if (!self) return null; // eski/serbest id → gizle
  const chain = [...svc.ancestors(categoryId), self];
  return chain.map((n) => ({ id: n.id, label: svc.label(n.id, lang) }));
}
```
> Not: `isGoogleId` import edildi ama burada zorunlu değil; ileride güvenlik kontrolü gerekirse hazır. Lint "unused" verirse import'tan çıkar.

### Komut + beklenen çıktı
```
npm test -- src/storefront/taxonomy/category-resolve.test.ts
```
Beklenen: tüm assertion yeşil.

### Commit
```
test+feat(taxonomy): resolveCategoryName + categoryBreadcrumb (store+market ortak)
```

---

## Task 4 — `manifest.ts` grup fallback (taksonomi-çözümlü "Other")

**Amaç:** `groupProductsByCategory` manifest invariant'ını (categories[].id ↔ products[].categoryId string eşitliği) **bozmadan**, çözülemeyenler gerçekten "Other"a düşsün; svc-çözümlü id'ler grup başlığı alabilsin. svc opsiyonel inject (geri uyumlu).

**Files:**
- DEĞİŞ: `src/storefront/manifest.ts`
- YENİ/DEĞİŞ: `src/storefront/manifest.test.ts`

### Adım 4.1 — Test (kırmızı)

`src/storefront/manifest.test.ts` (yeni testler ekle):
```ts
import { describe, it, expect } from 'vitest';
import { groupProductsByCategory } from './manifest';
import type { Manifest } from './types';

function mkManifest(partial: Partial<Manifest>): Manifest {
  return {
    store: { slug: 's', displayName: 'S', contact: {}, languages: ['en'], currency: 'USD' },
    categories: [],
    products: [],
    meta: { version: 1, updatedAt: '' },
    ...partial,
  } as Manifest;
}

describe('groupProductsByCategory — invariant korunur', () => {
  it('categoryId === category.id eşleşen ürünler doğru grupta', () => {
    const m = mkManifest({
      categories: [{ id: '267', name: { en: 'Phones' } }],
      products: [
        { id: 'p1', categoryId: '267', title: { en: 'A' }, images: [] },
        { id: 'p2', categoryId: '267', title: { en: 'B' }, images: [] },
      ],
    });
    const groups = groupProductsByCategory(m);
    expect(groups).toHaveLength(1);
    expect(groups[0].category?.id).toBe('267');
    expect(groups[0].products).toHaveLength(2);
  });

  it('eşleşmeyen categoryId → null (Other) grubuna düşer', () => {
    const m = mkManifest({
      categories: [{ id: '267', name: { en: 'Phones' } }],
      products: [{ id: 'p1', categoryId: 'c99', title: { en: 'X' }, images: [] }],
    });
    const groups = groupProductsByCategory(m);
    expect(groups).toHaveLength(1);
    expect(groups[0].category).toBeNull();
  });

  it('categoryId yoksa Other grubuna', () => {
    const m = mkManifest({
      categories: [],
      products: [{ id: 'p1', title: { en: 'X' }, images: [] }],
    });
    const groups = groupProductsByCategory(m);
    expect(groups[0].category).toBeNull();
  });

  it('boş gruplar elenir', () => {
    const m = mkManifest({
      categories: [{ id: '267', name: { en: 'Phones' } }],
      products: [],
    });
    expect(groupProductsByCategory(m)).toEqual([]);
  });
});
```
> Bu testler mevcut davranışı kilitler (regresyon güvenliği). Implementasyonun invariant'ı bozmadığını ispatlar.

### Adım 4.2 — Implementasyon

`manifest.ts:132-141` aynen korunur (invariant kritik). Tek değişiklik: `category-resolve`'ı re-export et ki tüketiciler tek yerden alsın.

`manifest.ts` sonuna ekle:
```ts
export { resolveCategoryName, categoryBreadcrumb } from './taxonomy/category-resolve';
export type { BreadcrumbSegment } from './taxonomy/category-resolve';
```

> Karar: `groupProductsByCategory` imzasını **değiştirmiyoruz**. Grup başlığı çözümü çağıran katmanda (`render.ts`, `CategorySection.astro`) `resolveCategoryName` ile yapılır; böylece svc inject yalnız render'da, manifest saf kalır ve mevcut tüm çağrılar kırılmaz.

### Komut + beklenen çıktı
```
npm test -- src/storefront/manifest.test.ts
```
Beklenen: yeni 4 test + varsa mevcut testler yeşil.

### Commit
```
test+refactor(manifest): grup invariant testleri + category-resolve re-export
```

---

## Task 5 — `marketplace.ts` label inject + breadcrumb + JSON-LD

**Amaç:** Ham id basan 4 fonksiyona `labelOf` callback inject; `renderCategoryPage` breadcrumb; `buildBreadcrumbJsonLd` helper. Saf kalır; svc router'dan gelir. **BUG FIX burada.**

**Files:**
- DEĞİŞ: `src/storefront/marketplace.ts`
- DEĞİŞ: `src/storefront/marketplace.test.ts`

### Adım 5.1 — Test (kırmızı)

`marketplace.test.ts` sonuna ekle:
```ts
import {
  renderCategoryChips,
  renderCategoryPage,
  buildBreadcrumbJsonLd,
  type LabelOf,
} from './marketplace';

const labelOf: LabelOf = (id, locale) => {
  const table: Record<string, Record<string, string>> = {
    en: { '267': 'Mobile Phones', '222': 'Electronics' },
    tr: { '267': 'Cep Telefonları', '222': 'Elektronik' },
  };
  return table[locale]?.[id] ?? table.en?.[id] ?? id;
};

describe('renderCategoryChips — label çözümü (BUG FIX)', () => {
  it('ham id yerine localized label basar', () => {
    const html = renderCategoryChips([{ id: '267', count: 3 }], 'tr', labelOf);
    expect(html).toContain('Cep Telefonları');
    expect(html).not.toContain('>267 <');
  });
  it('çözülemeyen id ham kalır (regresyon yok)', () => {
    const html = renderCategoryChips([{ id: 'c1', count: 1 }], 'en', labelOf);
    expect(html).toContain('c1');
  });
  it('href hâlâ numeric/ham id ile (kararlı URL)', () => {
    const html = renderCategoryChips([{ id: '267', count: 3 }], 'en', labelOf);
    expect(html).toContain('/market/c/267');
  });
});

describe('renderCategoryPage — breadcrumb + h1 label', () => {
  it('h1 leaf label, breadcrumb ara segmentler linkli', () => {
    const html = renderCategoryPage({
      categoryId: '267',
      items: [],
      total: 0,
      locale: 'en',
      breadcrumb: [
        { id: '222', label: 'Electronics' },
        { id: '267', label: 'Mobile Phones' },
      ],
    });
    expect(html).toContain('<h1 class="mk-section__title">Mobile Phones</h1>');
    expect(html).toContain('href="/market/c/222"');
    expect(html).toContain('Electronics');
  });
  it('breadcrumb null ise ham id h1\'de (kırılmaz)', () => {
    const html = renderCategoryPage({
      categoryId: 'electronics.phones', items: [], total: 0, locale: 'en', breadcrumb: null,
    });
    expect(html).toContain('electronics.phones');
  });
});

describe('buildBreadcrumbJsonLd', () => {
  it('BreadcrumbList üretir', () => {
    const json = buildBreadcrumbJsonLd(
      [{ id: '222', label: 'Electronics' }, { id: '267', label: 'Mobile Phones' }],
      'https://photozseo.com',
    );
    const obj = JSON.parse(json);
    expect(obj['@type']).toBe('BreadcrumbList');
    expect(obj.itemListElement).toHaveLength(2);
    expect(obj.itemListElement[0]).toMatchObject({
      position: 1, name: 'Electronics', item: 'https://photozseo.com/market/c/222',
    });
  });
});
```

### Adım 5.2 — Implementasyon

`marketplace.ts` — yeni tip + helper'lar:
```ts
import type { BreadcrumbSegment } from './taxonomy/category-resolve';

/** Router'dan inject edilen label çözücü (svc.label sarmalı). */
export type LabelOf = (id: string, locale: string) => string;

/** Varsayılan: kimlik (label yoksa ham id) — geri uyumlu test/edge. */
const identityLabel: LabelOf = (id) => id;
```

`renderCategoryChips` imzası + gövde:
```ts
export function renderCategoryChips(
  categories: { id: string; count: number }[],
  locale: string,
  labelOf: LabelOf = identityLabel,
): string {
  let html = `<nav class="mk-chips" aria-label="${escapeHtml(mt(locale, 'categories'))}">\n`;
  html += `  <a class="mk-chip mk-chip--all" href="/market/search">${escapeHtml(mt(locale, 'allCategories'))}</a>\n`;
  for (const c of categories) {
    const href = `/market/c/${encodeURIComponent(c.id)}`;
    const name = escapeHtml(labelOf(c.id, locale));
    html += `  <a class="mk-chip" href="${escapeAttr(href)}">${name} <span class="mk-chip__count">${c.count}</span></a>\n`;
  }
  html += '</nav>\n';
  return html;
}
```

`renderFacets` — kategori opt'ta `escapeHtml(c.id)` → `escapeHtml(labelOf(c.id, locale))`. `renderFacets` ve `renderSearchPage` imzalarına `labelOf: LabelOf = identityLabel` parametresi eklenir; `renderSearchPage` `renderFacets`'a forward eder.

`renderMarketHome` — `args`'a `labelOf?: LabelOf` ekle; Curated kartlarında `escapeHtml(c.id)` → `escapeHtml((labelOf ?? identityLabel)(c.id, locale))` (iki yer: img alt + overlay name).

`renderCategoryPage` — yeni imza:
```ts
export function renderCategoryPage(args: {
  categoryId: string;
  items: ProductRow[];
  total: number;
  locale: string;
  breadcrumb?: BreadcrumbSegment[] | null;
}): string {
  const { categoryId, items, locale, breadcrumb } = args;
  const segments = breadcrumb ?? null;
  const h1 = segments && segments.length
    ? segments[segments.length - 1].label
    : categoryId; // çözülemeyen: ham id (kırılmaz)

  let html = '<div class="mk">\n';
  html += '<header class="mk-top">\n';
  html += `  <a class="mk-logo" href="/market">${escapeHtml(mt(locale, 'marketTitle'))}</a>\n`;
  html += renderSearchBar(locale);
  html += renderLangSwitcher(locale);
  html += '</header>\n';

  // Breadcrumb: Market › L1 › L2 › … › leaf (ara segmentler linkli)
  html += `<nav class="mk-breadcrumb" aria-label="breadcrumb"><a href="/market">${escapeHtml(mt(locale, 'marketTitle'))}</a>`;
  if (segments && segments.length) {
    segments.forEach((seg, i) => {
      const last = i === segments.length - 1;
      if (last) {
        html += ` › <span>${escapeHtml(seg.label)}</span>`;
      } else {
        html += ` › <a href="/market/c/${encodeURIComponent(seg.id)}">${escapeHtml(seg.label)}</a>`;
      }
    });
  } else {
    html += ` › <span>${escapeHtml(categoryId)}</span>`;
  }
  html += '</nav>\n';

  html += '<section class="mk-section">\n';
  html += `  <h1 class="mk-section__title">${escapeHtml(h1)}</h1>\n`;
  if (items.length === 0) {
    html += `  <p class="mk-no-results">${escapeHtml(mt(locale, 'noResults'))}</p>\n`;
  } else {
    html += '  <div class="mk-grid">\n';
    for (const p of items) html += renderProductCard(p, locale);
    html += '  </div>\n';
  }
  html += '</section>\n';
  html += renderMarketFooter(locale);
  html += '</div>\n';
  return html;
}
```

`buildBreadcrumbJsonLd` (`buildItemListJsonLd` komşusu):
```ts
/** BreadcrumbList JSON-LD — /market/c/<id> segmentlerinden. JSON string (escape edilmemiş). */
export function buildBreadcrumbJsonLd(segments: BreadcrumbSegment[], origin: string): string {
  return JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: segments.map((s, i) => ({
      '@type': 'ListItem',
      position: i + 1,
      name: s.label,
      item: `${origin}/market/c/${encodeURIComponent(s.id)}`,
    })),
  });
}
```

### Komut + beklenen çıktı
```
npm test -- src/storefront/marketplace.test.ts
```
Beklenen: yeni label/breadcrumb/JSON-LD testleri + mevcut testler yeşil.

### Commit
```
test+feat(market): kategori label inject + breadcrumb + BreadcrumbList JSON-LD (raw-id bug fix)
```

---

## Task 6 — `render.ts` store breadcrumb (canlı P2 yolu)

**Amaç:** `renderProductBody`'ye breadcrumb satırı (`categoryBreadcrumb` → store `#cat-<id>` anchor linkli, kilitli D5=B); çözülemezse gizli. `renderStoreBody` grup başlığı `resolveCategoryName` ile. svc inject (opsiyonel — geri uyumlu).

**Files:**
- DEĞİŞ: `src/storefront/render.ts`
- DEĞİŞ: `src/storefront/render.test.ts`

### Adım 6.1 — Test (kırmızı)

`render.test.ts` sonuna ekle:
```ts
import { renderProductBody } from './render';
import { createTaxonomyService, type TaxNode } from './taxonomy/service';
import tree from './taxonomy/__fixtures__/tree.fixture.json';
import labelsEn from './taxonomy/__fixtures__/labels.en.fixture.json';
import type { Manifest } from './types';

const svc = createTaxonomyService({
  tree: tree as TaxNode[],
  labels: { en: labelsEn as Record<string, string> },
  taxonomyVersion: 1,
});

function mkM(catId: string | undefined): Manifest {
  return {
    store: { slug: 'shop', displayName: 'Shop', contact: {}, languages: ['en'], currency: 'USD' },
    categories: [],
    products: [{ id: 'p1', categoryId: catId, title: { en: 'Item' }, images: [], price: 10 }],
    meta: { version: 1, updatedAt: '' },
  } as Manifest;
}

describe('renderProductBody — store breadcrumb (#cat anchor, D5=B)', () => {
  it('çözülen kategori için breadcrumb + #cat-<id> anchor link', () => {
    const m = mkM('267');
    const html = renderProductBody(m, m.products[0], 'en', 'en', svc);
    expect(html).toContain('sf-breadcrumb');
    expect(html).toContain('href="/store/shop#cat-222"');
    expect(html).toContain('Mobile Phones');
  });
  it('çözülemeyen categoryId → breadcrumb satırı YOK (gizli)', () => {
    const m = mkM('electronics.phones');
    const html = renderProductBody(m, m.products[0], 'en', 'en', svc);
    expect(html).not.toContain('sf-breadcrumb');
  });
  it('svc verilmezse breadcrumb yok (geri uyumlu)', () => {
    const m = mkM('267');
    const html = renderProductBody(m, m.products[0], 'en', 'en');
    expect(html).not.toContain('sf-breadcrumb');
  });
});
```

### Adım 6.2 — Implementasyon

`render.ts` import:
```ts
import type { TaxonomyService } from './taxonomy/service';
import { categoryBreadcrumb, resolveCategoryName } from './taxonomy/category-resolve';
```

`renderProductBody` imzası: son parametre `svc?: TaxonomyService` ekle. `functions/store/[[path]].ts` bunu geçecek (Task 7'de wiring).

Seller chip'ten ÖNCE breadcrumb satırı (info panel başında):
```ts
// Breadcrumb (store içi anchor; çözülemezse gizli). storeSlug'a #cat-<id> anchor.
if (svc) {
  const segs = categoryBreadcrumb(product.categoryId, locale, svc);
  if (segs && segs.length) {
    html += '      <nav class="sf-breadcrumb" aria-label="breadcrumb">\n';
    html += `        <a href="${escapeAttr(storeUrl(store.slug, locale, defaultLang))}">${escapeHtml(store.displayName)}</a>\n`;
    for (const seg of segs) {
      const href = `${storeUrl(store.slug, locale, defaultLang)}#cat-${encodeURIComponent(seg.id)}`;
      html += `        <span class="sf-breadcrumb__sep">›</span><a href="${escapeAttr(href)}">${escapeHtml(seg.label)}</a>\n`;
    }
    html += '      </nav>\n';
  }
}
```
> Not: `storeUrl(slug, locale, defaultLang)` default dilde `/store/slug`, diğer dilde `/store/slug/<lang>`. Anchor `#cat-<id>` `renderStoreBody`'deki `id="cat-<id>"` ile eşleşir — store ana sayfasındaki ilgili bölüme atlatır (kilitli D5=B).

`renderStoreBody` grup başlığı: bugün `resolveLocalized(group.category.name, locale)` / `mt(locale,'other')`. svc varsa fallback'i güçlendir:
```ts
const heading = svc
  ? resolveCategoryName(group.category?.id, group.category?.name, locale, svc)
  : (group.category ? resolveLocalized(group.category.name, locale) : mt(locale, 'other'));
```
`renderStoreBody` imzasına da `svc?: TaxonomyService` eklenir.

### Komut + beklenen çıktı
```
npm test -- src/storefront/render.test.ts
```
Beklenen: 3 yeni breadcrumb testi + mevcut render testleri yeşil.

### Commit
```
test+feat(render): ProductDetail breadcrumb (#cat anchor) + grup başlığı taxonomy fallback
```

---

## Task 7 — Astro bileşenleri (build yolu aynası) + store router wiring

**Amaç:** `.astro` bileşenleri ve `functions/store/[[path]].ts`'i svc ile besle. Astro store sayfaları build/SSR'de **local adapter** (JSON import) kullanır — HTTP yok.

**Files:**
- DEĞİŞ: `src/components/storefront/CategorySection.astro`
- DEĞİŞ: `src/components/storefront/ProductDetail.astro`
- DEĞİŞ: `src/components/storefront/StorePage.astro`
- DEĞİŞ: `functions/store/[[path]].ts`
- YENİ: `src/storefront/taxonomy/load-local.ts` (local adapter — gerçek JSON import)

### Adım 7.1 — Local adapter

`src/storefront/taxonomy/load-local.ts`:
```ts
/**
 * load-local.ts — Local (static import) adapter. #1'in tree/labels/meta JSON'larını
 * import edip taxonomyService kurar. Store build/SSR + market edge tercih edilen yol
 * (HTTP yok; kilitli karar: web = static import). Lang label haritaları lazy: yalnız
 * istenen + en yüklenir.
 *
 * DEPENDENCY: tree.json/labels.*.json/meta.json #1'in build:taxonomy çıktısı.
 * Henüz yoksa bu modül import-time patlar → entegrasyon Task 12'de #1 sonrası bağlanır.
 */
import { createTaxonomyService, type TaxonomyService, type TaxNode } from './service';
import tree from './tree.json';
import meta from './meta.json';

const labelCache = new Map<string, Record<string, string>>();

async function loadLabels(lang: string): Promise<Record<string, string>> {
  if (labelCache.has(lang)) return labelCache.get(lang)!;
  try {
    const mod = await import(`./labels.${lang}.json`);
    const table = (mod.default ?? mod) as Record<string, string>;
    labelCache.set(lang, table);
    return table;
  } catch {
    return {};
  }
}

export async function getTaxonomyService(lang: string): Promise<TaxonomyService> {
  const langs = lang === 'en' ? ['en'] : [lang, 'en'];
  const labels: Record<string, Record<string, string>> = {};
  for (const l of langs) labels[l] = await loadLabels(l);
  return createTaxonomyService({
    tree: tree as TaxNode[],
    labels,
    taxonomyVersion: (meta as { taxonomyVersion: number }).taxonomyVersion,
  });
}
```
> Test edilmez (gerçek JSON import; #1 çıktısına bağlı). Saf mantık zaten Task 1-3'te fixture ile kapsandı. Bu sadece wiring.

### Adım 7.2 — `functions/store/[[path]].ts` wiring

Import ekle:
```ts
import { getTaxonomyService } from '../../src/storefront/taxonomy/load-local';
```
`renderStoreBody`/`renderProductBody` çağrılarından önce svc kur (graceful — patlarsa undefined geç):
```ts
let svc;
try { svc = await getTaxonomyService(locale); } catch { svc = undefined; }
```
Çağrıları güncelle: `renderProductBody(manifest, product, locale, DEFAULT_LANG, svc)` ve `renderStoreBody(manifest, locale, DEFAULT_LANG, svc)`.

> Önemli: `getStore`'dan dönen manifest'in `categories[].id` + `products[].categoryId`'si **lazy map** ile çevrilmeli ki render Google id görsün (Task 10 ile aynı katman). Store render KV'den okuyor; market D1'den. İki ayrı okuma yolu → ikisinde de map uygulanır. Store için: `getStore` sonrası manifest'i normalize eden saf `applyLegacyMapToManifest(manifest, legacyMap)` helper'ı Task 10'da eklenir; burada çağrılır:
```ts
import { applyLegacyMapToManifest } from '../_lib/taxonomy-migrate';
import legacyMap from '../../src/storefront/taxonomy/legacy-map.json';
// getStore sonrası, çeviriden önce:
const mappedManifest = applyLegacyMapToManifest(record.manifest, legacyMap as Record<string, string>);
// base = mappedManifest;
```

### Adım 7.3 — Astro bileşenleri

`CategorySection.astro` — `resolveCategoryName` kullan (svc prop ile):
```astro
---
import type { CategoryGroup } from '../../storefront/manifest';
import { resolveCategoryName } from '../../storefront/manifest';
import type { TaxonomyService } from '../../storefront/taxonomy/service';
import ProductCard from './ProductCard.astro';

interface Props {
  group: CategoryGroup; locale: string; currency: string;
  slug: string; defaultLang: string; slugMap: Map<string, string>;
  svc?: TaxonomyService;
}
const { group, locale, currency, slug, defaultLang, slugMap, svc } = Astro.props;
const heading = svc
  ? resolveCategoryName(group.category?.id, group.category?.name, locale, svc)
  : (group.category ? (group.category.name[locale] ?? group.category.name.en) : (locale === 'tr' ? 'Diğer' : 'Other'));
---
```
(şablonun geri kalanı aynı; `{heading}` kullanır)

`ProductDetail.astro` — breadcrumb bloğu ekle (info başında):
```astro
---
// ... mevcut importlar
import { categoryBreadcrumb } from '../../storefront/manifest';
import type { TaxonomyService } from '../../storefront/taxonomy/service';
// Props'a svc?: TaxonomyService ekle
const segs = svc ? categoryBreadcrumb(product.categoryId, locale, svc) : null;
const storeBase = storeUrl(manifest.store.slug, locale, defaultLang);
---
<!-- sf-detail__info başında -->
{segs && segs.length > 0 && (
  <nav class="sf-breadcrumb" aria-label="breadcrumb">
    <a href={storeBase}>{manifest.store.displayName}</a>
    {segs.map((seg) => (
      <><span class="sf-breadcrumb__sep">›</span><a href={`${storeBase}#cat-${encodeURIComponent(seg.id)}`}>{seg.label}</a></>
    ))}
  </nav>
)}
```
> `categoryBreadcrumb` `null` ise blok hiç render edilmez (gizli). `storeUrl` import'u manifest'ten alınır.

`StorePage.astro` — `getTaxonomyService` ile svc kur, `CategorySection`'a geçir:
```astro
---
import { getTaxonomyService } from '../../storefront/taxonomy/load-local';
const svc = await getTaxonomyService(locale).catch(() => undefined);
---
{groups.map((group) => (
  <CategorySection group={group} locale={locale} currency={manifest.store.currency} slug={slug} defaultLang={defaultLang} slugMap={slugMap} svc={svc} />
))}
```

### Komut + beklenen çıktı
```
npx astro check
```
Beklenen: 0 error (svc opsiyonel olduğu için tip güvenli). Astro build'i #1 JSON'ları gelene kadar `load-local` import-time patlayabilir → bu yüzden bileşenlerde `.catch(() => undefined)`.

### Commit
```
feat(store): astro bileşenleri + store router svc wiring + local adapter (breadcrumb/label)
```

---

## Task 8 — `functions/market/[[path]].ts` wiring (label inject + facet drill + breadcrumb JSON-LD)

**Amaç:** Market router taxonomyService'i kurar, `labelOf` inject eder, `/market/c/<id>`'de breadcrumb + BreadcrumbList JSON-LD üretir, facet drill-down reduce (L1+L2). Kategori chip'leri **children(null) = L1 kökler** ile beslenir.

**Files:**
- DEĞİŞ: `functions/market/[[path]].ts`

### Adım 8.1 — Wiring (test: mevcut router testi varsa genişlet; yoksa entegrasyon Task 12)

Import:
```ts
import { getTaxonomyService } from '../../src/storefront/taxonomy/load-local';
import { buildBreadcrumbJsonLd, type LabelOf } from '../../src/storefront/marketplace';
import { categoryBreadcrumb } from '../../src/storefront/taxonomy/category-resolve';
```

`handleMarket` başında svc + labelOf:
```ts
let svc;
try { svc = await getTaxonomyService(locale); } catch { svc = undefined; }
const labelOf: LabelOf = (id, loc) => (svc ? svc.label(id, loc) : id);
```

`/market` (home): `renderMarketHome({ ..., labelOf })`. Ayrıca chip kaynağını **L1 köklerle** zenginleştir (kilitli L1+L2 drill): facet'ten gelen distinct category_id'lerin **L1 atalarını** topla, böylece ana sayfa chip'leri L1 gösterir:
```ts
// cats: distinct category_id'lerin L1 atası (drill başlangıcı)
const l1 = new Map<string, number>();
for (const p of newP.items) {
  if (!p.category_id) continue;
  const root = svc ? (svc.ancestors(p.category_id)[0]?.id ?? p.category_id) : p.category_id;
  l1.set(root, (l1.get(root) ?? 0) + 1);
}
const cats = [...l1.entries()].map(([id, count]) => ({ id, count }));
```

`/market/search`: `renderSearchPage({ ..., })` — `renderSearchPage`/`renderFacets`'a `labelOf` parametresi forward edilir (Task 5'te imza eklendi).

`/market/c/<id>` (drill + breadcrumb):
```ts
if (parts[0] === 'c' && parts.length === 2) {
  const categoryId = decodeURIComponent(parts[1]);
  const result = await deps.searchProducts(deps.db, deps.ai as AiBinding, { categoryId, lang: locale, sort: 'new', limit: 40 });
  const breadcrumb = svc ? categoryBreadcrumb(categoryId, locale, svc) : null;
  const body = renderCategoryPage({ categoryId, items: result.items, total: result.total, locale, breadcrumb });

  const title = (breadcrumb && breadcrumb.length ? breadcrumb[breadcrumb.length - 1].label : categoryId) + ' — ' + mt(locale, 'marketTitle');
  const itemListLd = buildItemListJsonLd(result.items, origin);
  const jsonLd = breadcrumb && breadcrumb.length
    ? `${buildBreadcrumbJsonLd(breadcrumb, origin)}</script><script type="application/ld+json">${itemListLd}`
    : itemListLd;
  // not: tek jsonLd slot var → iki schema'yı ardışık script ile bas (document.ts safeJsonLd </script> kaçışı korunur)

  return htmlResponse(renderDocument({
    title, description: mt(locale, 'trustBadge'),
    lang: locale, body,
    canonical: `${origin}/market/c/${encodeURIComponent(categoryId)}`,
    alternates: buildAlternates(origin, `/market/c/${encodeURIComponent(categoryId)}`),
    jsonLd,
    stylesheets: ['/marketplace.css?v=2'],
    bodyScripts: ['/marketplace-enhance.js'],
  }));
}
```
> **Karar:** İki JSON-LD blok için en temiz yol `document.ts`'e ikinci `jsonLd2` alanı eklemek. Yukarıdaki inline kaçış kırılganlığını önlemek için **alternatif (tercih edilen):** `document.ts`'e `jsonLd?: string | string[]` desteği ekle, dizi verince her biri ayrı `<script type="application/ld+json">` basılsın. Bu küçük değişikliği Task 8'e dahil et:
```ts
// document.ts
if (jsonLd) {
  const blocks = Array.isArray(jsonLd) ? jsonLd : [jsonLd];
  for (const b of blocks) head += `\n<script type="application/ld+json">${safeJsonLd(b)}</script>`;
}
```
Ve `DocumentOptions.jsonLd: string | string[]`. Sonra router'da `jsonLd: breadcrumb?.length ? [buildBreadcrumbJsonLd(...), itemListLd] : itemListLd`.

### Komut + beklenen çıktı
```
npx tsc --noEmit -p tsconfig.json
npm test
```
Beklenen: tip hatasız; tüm test suite yeşil (yeni + mevcut). `Test Files  N passed`.

### Commit
```
feat(market): router label inject + L1 facet drill + breadcrumb JSON-LD; document.ts çoklu jsonLd
```

---

## Task 9 — Sitemap: ürün-barındıran L1 kategori URL'leri

**Amaç:** `/market/c/<L1-id>` URL'lerini sitemap'e ekle (kilitli D4=B). Sadece **ürün barındıran** L1 (distinct category_id → L1 ata). Saf helper test edilir.

**Files:**
- YENİ: `src/storefront/market-sitemap.ts`
- YENİ: `src/storefront/market-sitemap.test.ts`
- DEĞİŞ: `functions/marketplace-sitemap.ts`

### Adım 9.1 — Test (kırmızı)

`src/storefront/market-sitemap.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { productBearingL1Urls } from './market-sitemap';
import { createTaxonomyService, type TaxNode } from './taxonomy/service';
import tree from './taxonomy/__fixtures__/tree.fixture.json';

const svc = createTaxonomyService({
  tree: tree as TaxNode[], labels: { en: {} }, taxonomyVersion: 1,
});

describe('productBearingL1Urls', () => {
  it('distinct category_id\'lerin L1 atalarını URL\'ye çevirir', () => {
    const urls = productBearingL1Urls(['267', '187'], svc, 'https://photozseo.com');
    // 267 → L1 ata 222; 187 → L1 ata 166
    expect(urls).toContain('https://photozseo.com/market/c/222');
    expect(urls).toContain('https://photozseo.com/market/c/166');
  });
  it('aynı L1\'e düşen id\'ler tekilleşir', () => {
    const urls = productBearingL1Urls(['267', '262'], svc, 'https://photozseo.com');
    // 267→222, 262→222 → tek URL
    expect(urls.filter((u) => u.endsWith('/c/222'))).toHaveLength(1);
  });
  it('zaten L1 olan id kendini verir', () => {
    const urls = productBearingL1Urls(['222'], svc, 'https://x.com');
    expect(urls).toEqual(['https://x.com/market/c/222']);
  });
  it('çözülemeyen id atlanır (kırılmaz)', () => {
    const urls = productBearingL1Urls(['electronics.phones'], svc, 'https://x.com');
    expect(urls).toEqual([]);
  });
});
```

### Adım 9.2 — Implementasyon

`src/storefront/market-sitemap.ts`:
```ts
/**
 * market-sitemap.ts — Ürün-barındıran L1 kategori URL'leri (saf, svc inject).
 * Kilitli karar D4=B: yalnız ürünlü üst-seviye (L1) kategoriler sitemap'e girer.
 * Çözülemeyen id'ler atlanır (thin-content/crawl israfı önlenir).
 */
import type { TaxonomyService } from './taxonomy/service';

/** distinct category_id listesini → benzersiz L1 /market/c/<id> URL dizisine indir. */
export function productBearingL1Urls(
  categoryIds: string[],
  svc: TaxonomyService,
  origin: string,
): string[] {
  const l1 = new Set<string>();
  for (const id of categoryIds) {
    if (!id) continue;
    if (!svc.node(id)) continue; // çözülemeyen → atla
    const ancestors = svc.ancestors(id);
    const rootId = ancestors.length ? ancestors[0].id : id; // depth-0 ata, yoksa kendisi
    l1.add(rootId);
  }
  return [...l1].map((id) => `${origin}/market/c/${encodeURIComponent(id)}`);
}
```

`functions/marketplace-sitemap.ts` — `buildMarketplaceSitemap` çağrısı sonrası L1 URL'leri ekle:
```ts
import { productBearingL1Urls } from '../src/storefront/market-sitemap';
import { getTaxonomyService } from '../src/storefront/taxonomy/load-local';
// onRequestGet içinde:
const distinctCats = [...new Set(products.items.map((p) => p.category_id).filter(Boolean))];
let catUrls: string[] = [];
try {
  const svc = await getTaxonomyService('en');
  catUrls = productBearingL1Urls(distinctCats, svc, origin);
} catch { /* taxonomy yoksa kategori URL'siz sitemap (graceful) */ }
const xml = buildMarketplaceSitemap(stores.items, products.items, origin, catUrls);
```
`buildMarketplaceSitemap` imzasına `extraUrls: string[] = []` ekle; `urls.push(...extraUrls)`.

### Komut + beklenen çıktı
```
npm test -- src/storefront/market-sitemap.test.ts
```
Beklenen: 4 test yeşil.

### Commit
```
test+feat(sitemap): ürün-barındıran L1 kategori URL'leri (graceful taxonomy fallback)
```

---

## Task 10 — Lazy legacy-map okuma (market D1 + manifest helper)

**Amaç:** D1 okuma yolunda (market) ham `category_id` lazy çevrilsin; store KV yolunda manifest (categories[]+products[].categoryId **birlikte**) çevrilsin. Saf `applyLegacyMapToManifest` + Orama'ya kategori label gömme.

**Files:**
- YENİ: `functions/_lib/taxonomy-migrate.ts`
- YENİ: `functions/_lib/taxonomy-migrate.test.ts`
- DEĞİŞ: `functions/_lib/marketplace.ts` (storeRecordToProductRows category_id map + Orama kategori label)

### Adım 10.1 — Test (kırmızı)

`functions/_lib/taxonomy-migrate.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { applyLegacyMapToManifest } from './taxonomy-migrate';
import type { Manifest } from '../../src/storefront/types';

const map = { 'electronics.phones': '267', 'clothing.shoes': '187' };

function mk(): Manifest {
  return {
    store: { slug: 's', displayName: 'S', contact: {}, languages: ['en'], currency: 'USD' },
    categories: [
      { id: 'electronics.phones', name: { en: 'Phones' } },
      { id: 'c1', name: { en: 'Custom' } },
    ],
    products: [
      { id: 'p1', categoryId: 'electronics.phones', title: { en: 'A' }, images: [] },
      { id: 'p2', categoryId: 'c1', title: { en: 'B' }, images: [] },
      { id: 'p3', categoryId: '187', title: { en: 'C' }, images: [] },
    ],
    meta: { version: 1, updatedAt: '' },
  } as Manifest;
}

describe('applyLegacyMapToManifest — invariant korunarak birlikte çevirir', () => {
  it('categories[].id + products[].categoryId AYNI haritayla çevrilir (bağ korunur)', () => {
    const out = applyLegacyMapToManifest(mk(), map);
    expect(out.categories[0].id).toBe('267');
    expect(out.products[0].categoryId).toBe('267'); // p1 hâlâ category 267 ile eşleşir
  });
  it('serbest id (c1) → uncategorized sentinel (categories + products tutarlı)', () => {
    const out = applyLegacyMapToManifest(mk(), map);
    expect(out.categories[1].id).toBe('uncategorized');
    expect(out.products[1].categoryId).toBe('uncategorized');
  });
  it('zaten google id (187) dokunulmaz (idempotent)', () => {
    const out = applyLegacyMapToManifest(mk(), map);
    expect(out.products[2].categoryId).toBe('187');
  });
  it('iki kez uygulamak idempotent (no-op)', () => {
    const once = applyLegacyMapToManifest(mk(), map);
    const twice = applyLegacyMapToManifest(once, map);
    expect(twice).toEqual(once);
  });
  it('orijinal manifest mutate edilmez (pure)', () => {
    const m = mk();
    applyLegacyMapToManifest(m, map);
    expect(m.products[0].categoryId).toBe('electronics.phones');
  });
});
```

### Adım 10.2 — Implementasyon

`functions/_lib/taxonomy-migrate.ts`:
```ts
/**
 * taxonomy-migrate.ts — Lazy (read-time) legacy id → google id çevirisi.
 * #5 dual-write'ın read-map yarısı. categories[] + products[].categoryId BİRLİKTE
 * çevrilir (manifest.ts invariant'ı: string-eşitlik bağı korunmalı). Saf, non-destructive
 * (yeni nesne döner, orijinali mutate etmez). Çözülemeyen → uncategorized sentinel + warn.
 */
import type { Manifest } from '../../src/storefront/types';
import { mapLegacyId } from '../../src/storefront/taxonomy/legacy-map';

export function applyLegacyMapToManifest(
  manifest: Manifest,
  map: Record<string, string>,
  warn?: (oldId: string) => void,
): Manifest {
  const mapped = (id: string | undefined): string => {
    const g = mapLegacyId(id, map);
    if (id && g === 'uncategorized' && warn) warn(id);
    return g;
  };
  return {
    ...manifest,
    categories: manifest.categories.map((c) => ({ ...c, id: mapped(c.id) })),
    products: manifest.products.map((p) => ({
      ...p,
      categoryId: p.categoryId != null ? mapped(p.categoryId) : p.categoryId,
    })),
  };
}
```

`functions/_lib/marketplace.ts` — `storeRecordToProductRows` D1'e yazmadan önce category_id'yi çevir (KV→D1 sync zaten okuma; burada normalize edilir → D1 Google id tutar):
```ts
import legacyMap from '../../src/storefront/taxonomy/legacy-map.json';
import { mapLegacyId } from '../../src/storefront/taxonomy/legacy-map';
// storeRecordToProductRows içinde:
category_id: p.categoryId ? mapLegacyId(p.categoryId, legacyMap as Record<string, string>) : '',
```
> Bu, D1'e yazılan satırların **Google id** taşımasını sağlar (market facet'leri, /market/c/<id> doğrudan numeric çalışır). Store KV okuma yolu ise `applyLegacyMapToManifest` (Task 7.2) ile çevrilir. İki yol da aynı `legacy-map.json` + aynı `mapLegacyId`.

`buildOrama` — kategori label'larını docs'a göm (kilitli: tek indekse göm, Orama deep search kategori adıyla eşleşsin):
```ts
import { getTaxonomyService } from '../../src/storefront/taxonomy/load-local';
// buildOrama içinde, schema'ya 'categoryPath' alanı ekle:
schema: { id: 'string', title: 'string', description: 'string', tags: 'string', categoryPath: 'string' },
// svc ile her ürünün kategori yol-label'larını (aktif kanonik dilde) göm:
let svc; try { svc = await getTaxonomyService('en'); } catch { svc = undefined; }
const docs = products.map((p) => ({
  id: p.id, title: p.title, description: p.description,
  tags: p.tags.replace(/,/g, ' '),
  categoryPath: svc && p.category_id ? svc.path(p.category_id, 'en').join(' ') : '',
}));
```
> "running shoes" araması ürün kategori yol-label'ıyla da eşleşir (kilitli D3=A).

### Komut + beklenen çıktı
```
npm test -- functions/_lib/taxonomy-migrate.test.ts
```
Beklenen: 5 test yeşil (idempotency + invariant + pure dahil).

### Commit
```
test+feat(market): lazy legacy-map okuma (manifest+D1) + Orama kategori-yol gömme
```

---

## Task 11 — Write-normalize (dual-write savunma katmanı)

**Amaç:** `putStore` çağrılmadan önce manifest Google id'ye normalize edilsin (idempotent). Eski iOS sürümü eski id gönderse bile KV temizlenir (kilitli: dual-write). Backfill (B-lite) **bu sürümde kapsam dışı** (kilitli öneri: lazy+write-normalize yeterli; backfill ertelenir).

**Files:**
- DEĞİŞ: `functions/_lib/registry.ts`
- DEĞİŞ: `functions/_lib/taxonomy-migrate.test.ts` (write-normalize damga testi)

### Adım 11.1 — Test (kırmızı)

`taxonomy-migrate.test.ts`'e ekle:
```ts
import { normalizeManifestForWrite } from './taxonomy-migrate';

describe('normalizeManifestForWrite — write-normalize + damga', () => {
  it('eski id\'leri çevirir ve taxonomyMigration damgası ekler', () => {
    const out = normalizeManifestForWrite(mk(), map, 7);
    expect(out.products[0].categoryId).toBe('267');
    expect(out.meta.taxonomyVersion).toBe(7);
    expect(out.meta.taxonomyMigration?.unmapped).toEqual(['c1']);
    expect(out.meta.taxonomyMigration?.toVersion).toBe(7);
  });
  it('zaten normalize manifest tekrar yazılırsa idempotent (unmapped boş, içerik aynı)', () => {
    const once = normalizeManifestForWrite(mk(), map, 7);
    const twice = normalizeManifestForWrite(once, map, 7);
    expect(twice.products).toEqual(once.products);
    expect(twice.categories).toEqual(once.categories);
  });
});
```
> `ManifestMeta`'ya `taxonomyMigration?: { fromVersion?: number; toVersion: number; migratedAt: string; unmapped: string[] }` alanı eklenir (`types.ts`).

### Adım 11.2 — Implementasyon

`types.ts` — `ManifestMeta`'ya ekle:
```ts
  taxonomyMigration?: {
    fromVersion?: number;
    toVersion: number;
    migratedAt: string;
    unmapped: string[];
  };
```

`taxonomy-migrate.ts` — `normalizeManifestForWrite`:
```ts
export function normalizeManifestForWrite(
  manifest: Manifest,
  map: Record<string, string>,
  toVersion: number,
): Manifest {
  const unmapped: string[] = [];
  const mapped = applyLegacyMapToManifest(manifest, map, (oldId) => {
    if (!unmapped.includes(oldId)) unmapped.push(oldId);
  });
  return {
    ...mapped,
    meta: {
      ...mapped.meta,
      taxonomyVersion: toVersion,
      taxonomyMigration: {
        fromVersion: manifest.meta.taxonomyVersion,
        toVersion,
        migratedAt: new Date().toISOString(),
        unmapped,
      },
    },
  };
}
```

`registry.ts` — `putStore`'da normalize uygula:
```ts
import { normalizeManifestForWrite } from './taxonomy-migrate';
import legacyMap from '../../src/storefront/taxonomy/legacy-map.json';
import meta from '../../src/storefront/taxonomy/meta.json';

export async function putStore(kv: KVNamespace, slug: string, record: StoreRecord): Promise<void> {
  let toWrite = record;
  try {
    const tv = (meta as { taxonomyVersion: number }).taxonomyVersion;
    const normalized = normalizeManifestForWrite(record.manifest, legacyMap as Record<string, string>, tv);
    toWrite = { ...record, manifest: normalized };
  } catch { /* taxonomy yoksa ham yaz (graceful) */ }
  await kv.put(storeKey(slug), JSON.stringify(toWrite));
}
```
> `meta.json` import #1 çıktısına bağlı → `try/catch` graceful. #1 gelene kadar `putStore` ham yazar (mevcut davranış); geldiğinde otomatik normalize başlar. Lazy okuma (Task 10) zaten doğru render verdiği için bu güvenli.

### Komut + beklenen çıktı
```
npm test -- functions/_lib/taxonomy-migrate.test.ts
```
Beklenen: write-normalize 2 yeni test + Task 10'un 5 testi = 7 yeşil.

### Commit
```
test+feat(registry): write-normalize (dual-write) + taxonomyMigration damgası; meta tipi
```

---

## Task 12 — Entegrasyon doğrulama (#1 çıktısı geldiğinde)

**Amaç:** `build:taxonomy` çalışıp gerçek `tree.json/labels.*.json/meta.json` geldikten sonra uçtan uca doğrulama. Bu görev **#1 bağımlı** — fixture testleri zaten geçiyor; bu sadece gerçek veriyle smoke.

**Files:** (kod değişikliği yok; doğrulama)

### Adımlar
1. `npm run build:taxonomy` çalıştır (#1 script). Beklenen: `src/storefront/taxonomy/{tree,labels.*,meta}.json` oluşur.
2. `npm test` — artık `legacy-map.test.ts`'teki **tree doğrulama testi aktifleşir** (skip değil). Beklenen: tüm 31 hedef tree'de → yeşil. Eksik varsa CI fail → tablo düzeltilir (riskli id'ler: 267, 499713, 505370, 5424, 2882).
3. `npm run build` (astro) — `load-local` gerçek JSON import eder, import-time patlamaz. Beklenen: build başarılı.
4. `npx tsc --noEmit` — tip hatasız.
5. Manuel smoke (wrangler pages dev): `/market/c/267` → h1 "Mobile Phones" (ham id DEĞİL), breadcrumb "Marketplace › Electronics › … › Mobile Phones", her segment link. `/store/<slug>/product/<p>` → breadcrumb satırı (#cat anchor). Eski id'li store → ürünler doğru Google grubunda, hiçbiri beklenmedik "Other"da değil.

### Komut + beklenen çıktı
```
npm run build:taxonomy && npm test && npm run build
```
Beklenen: 3 komut da exit 0; tree doğrulama testi `passed` (skip değil); build başarılı.

### Commit
```
chore(taxonomy): #1 entegrasyonu doğrulandı (tree doğrulama aktif, build yeşil)
```

---

## Self-Review

**Kapsanan gereksinimler:**
- ✅ Paylaşılan saf-TS `taxonomyService` (node/children/ancestors/label/path/version) — Task 1, fixture ile test.
- ✅ Lazy `legacy-map.json` (old→google) read-time, **hem `categories[]` hem `products[].categoryId`** birlikte — Task 10 (`applyLegacyMapToManifest`), invariant testli.
- ✅ Market L1+L2 browse façade (children-based chip + drill) + Orama deep search (kategori yol-label gömme) — Task 8, 10.
- ✅ Store breadcrumb (parentId zinciri, `#cat-<id>` anchor, D5=B) — Task 6 (render.ts canlı yol) + Task 7 (ProductDetail.astro aynası).
- ✅ Graceful "uncategorized" fallback, hiçbir yerde throw yok — service/category-resolve/legacy-map hepsi id'ye/sentinel'e düşer; testlerle kanıtlı.
- ✅ Localized label'lar — labels.<lang> + en fallback + id fallback.
- ✅ BreadcrumbList JSON-LD (market + store) + sitemap ürün-barındıran L1 — Task 5, 8, 9.
- ✅ **Raw-id bug fix** — Task 5 (renderCategoryChips/Facets/CategoryPage/MarketHome label inject).
- ✅ Web = static import (HTTP değil) — `load-local.ts` adapter; #2 API kontratı referans ama tüketilmiyor (kilitli karar).
- ✅ Dual-write — lazy okuma (Task 10) + write-normalize (Task 11); backfill bilinçli ertelendi (kilitli öneri).

**Dependency caveat'ler:**
1. **#1 (`build:taxonomy`) zorunlu önkoşul.** `src/storefront/taxonomy/{tree,labels,meta}.json` henüz yok. Tüm saf mantık `__fixtures__/` ile test edilir (geçer); `load-local.ts` + `meta.json`/`legacy-map` tree-doğrulama gerçek veri gelince çalışır. Task 1-11 #1 olmadan tamamlanır ve test geçer; Task 12 #1 sonrası smoke.
2. **legacy-map google id'leri doğrulanmamış.** Tablodaki riskli id'ler (267, 499713, 505370, 5424, 2882) `tree.json`'a karşı CI hard-stop testiyle (Task 2.4) #1 sonrası doğrulanır; eksikse fail + tablo düzeltme.
3. **iOS (#4) ayrı repo.** `legacy-map.json` web+iOS ortak; bu plan yalnız web kopyasını üretir. iOS bundle koordinasyonu #4'ün işi.
4. **Atomiklik:** #3 render + #5 lazy-map aynı branch/sürümde gider (kilitli). Bu plan ikisini tek görev zincirinde topladı; ayrı merge edilmemeli.

**Riskler / dikkat:**
- `groupProductsByCategory` invariant'ı (manifest.ts:132-141) **kasıtlı değiştirilmedi**; çözüm çağıran katmanda. Test 4 invariant'ı kilitler.
- `document.ts` çoklu jsonLd değişikliği (Task 8) mevcut tek-string çağrıları kırmaz (`string | string[]` geri uyumlu).
- `load-local` import-time hataları her wiring noktasında `try/catch`/`.catch(() => undefined)` ile graceful; svc yoksa eski (ham id) davranışa düşer → regresyon yok.
- Astro bileşenleri ile `render.ts` **çift bakım**: ikisi de güncellendi (Task 6+7); canlı P2 yolu `render.ts`.

**Çalıştırma:** Tüm test komutu `npm test` (vitest, `src/**/*.test.ts` + `functions/**/*.test.ts`). Tip: `npx tsc --noEmit`. Astro: `npx astro check` / `npm run build`.
